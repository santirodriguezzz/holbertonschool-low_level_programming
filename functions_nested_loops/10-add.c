#include "main.h"
/**
 * add - funcion que suma dos numeros enteros y devuelve el resultado
 * @a: variable que almacena el primer int que se agrega
 * @b: variable que alacena el segundo int que se agrega
 * Return: resultado operacion
 */
int add(int a, int b)
{
	int res;

	res = a + b;
	return (res);
}
